手動更新 cashbooktw/cashbooktw.github.io 今日的非 Facebook edition 內容，使用 GitHub connector。先讀固定 `master` 上的 `automation/UPDATE_RULES.md`、`config/sources.json`、`schema/edition.schema.json` 與 `data/index.json`，再依 UPDATE_RULES 逐一讀取已設定的公開非 Facebook 來源完整原文並整理新資訊。讀不到完整原文的項目不得摘要或編造，來源限制須寫入 `source_reports.configured_sources`；保留現有 Facebook stories、`original_text`、source report 與其他所有正式 stories，不做跨 Facebook／非 Facebook 通道的語意去重。正常更新只改當日 `data/editions/YYYY-MM-DD.json` 和 `data/index.json`，用 Git Data API 對固定 `master` 原子更新同一 commit；遇 HEAD 競爭時重讀合併，最多重試三次，禁止順序寫入 fallback。驗證 JSON 與 schema 後提交並重讀驗證。不要刪除舊 story、圖片引用或歷史 edition，不新增 repo 圖片。最後回報日期、story 數、各通道狀態/讀取限制、commit SHA；無法讀取或寫入時如實回報，不宣稱完成。Facebook Favorites 由桌面 Codex 的既有本機 CLI 擷取，本任務不得自行擷取或替代該程式。
